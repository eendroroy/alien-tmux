tmux:
    symbols:
        branch: '#{@alien_gitmux_branch_symbol}'
        hashprefix: '#{@alien_gitmux_hashprefix_symbol}'
        ahead: '#{@alien_gitmux_ahead_symbol}'
        behind: '#{@alien_gitmux_behind_symbol}'
        staged: '#{@alien_gitmux_staged_symbol}'
        conflict: '#{@alien_gitmux_conflict_symbol}'
        modified: '#{@alien_gitmux_modified_symbol}'
        untracked: '#{@alien_gitmux_untracked_symbol}'
        stashed: '#{@alien_gitmux_stashed_symbol}'
        clean: '#{@alien_gitmux_clean_symbol}'
        insertions: '#{@alien_gitmux_insertions_symbol}'
        deletions: '#{@alien_gitmux_deletions_symbol}'
    styles:
        clear: '#[fg=#{@alien_gitmux_clear_fg}]'
        state: '#[fg=#{@alien_gitmux_state_fg}]'
        branch: '#[fg=#{@alien_gitmux_branch_fg}]'
        remote: '#[fg=#{@alien_gitmux_remote_fg}]'
        divergence: '#[fg=#{@alien_gitmux_divergence_fg}]'
        staged: '#[fg=#{@alien_gitmux_staged_fg}]'
        conflict: '#[fg=#{@alien_gitmux_conflict_fg}]'
        modified: '#[fg=#{@alien_gitmux_modified_fg}]'
        untracked: '#[fg=#{@alien_gitmux_untracked_fg}]'
        stashed: '#[fg=#{@alien_gitmux_stashed_fg}]'
        clean: '#[fg=#{@alien_gitmux_clean_fg}]'
        insertions: '#[fg=#{@alien_gitmux_insertions_fg}]'
        deletions: '#[fg=#{@alien_gitmux_deletions_fg}]'
#    layout: [branch, remote-branch, divergence, flags, stats]
    layout: [' ', branch, divergence, flags, ' ']
    options:
        branch_max_len: 0
        branch_trim: right
        ellipsis: …
        hide_clean: false
        swap_divergence: false
        divergence_space: false